spring:
  datasource:
    url: ${database_url}reporting_test?useSSL=false&useLegacyDatetimeCode=false
    password: ${database_password}
    username: ${database_user}

artifacts:
  root: "classpath:"

permissionservice:
  endpoint:

application:
  state: CA

app:
  iris:
    vendorId: vendorId
    url: http://my-iris.somewhere.org/iris/